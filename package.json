{
  "name": "splyza-video-box-rest-api",
  "version": "1.0.0",
  "description": "mockup rest api for testing",
  "engines": {
    "node": "16.16.0",
    "npm": "8.11.0"
  },
  "scripts": {
    "start": "npm run clean && npm run build && npm run serve",
    "serve": "node ./dist/app.js",
    "clean": "rimraf dist",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "guillermo.martel@splyza.com",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.14",
    "@types/express": "^4.17.17",
    "typescript": "^5.2.2"
  }
}
